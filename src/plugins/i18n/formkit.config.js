import { sr } from '@formkit/i18n'
import { rootClasses } from '/formkit.theme.mjs'

const config = {
    locales: { sr },
    locale: 'sr',
    config: {
        rootClasses
    }
}

export default config
